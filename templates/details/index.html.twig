{% extends 'base.html.twig' %}

{% block title %}Plus de détails sur votre covoiturages{% endblock %}

{% block body %}
<script>
    // Vérifiez si l'utilisateur est connecté et passez les données au script
    window.currentUser  = {{ user ? {
        id: user.id,
        
        roles: user.roles 
    }|json_encode|raw : 'null' }};
     window.covoiturageId = {{ covoiturage_id|json_encode|raw }};
     
</script>
<div {{ react_component('DetailVue') }}></div>
{% endblock %}
