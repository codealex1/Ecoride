{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<script>
    // Vérifiez si l'utilisateur est connecté et passez les données au script
    window.currentUser  = {{ user ? {
        id: user.id,
        
        roles: user.roles 
    }|json_encode|raw : 'null' }};
    
</script>

<div {{ react_component('Accueil') }}></div>
            
        
    
{% endblock %}
